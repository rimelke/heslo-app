import Input from "@components/form/Input";

const TextInput = () => (
  <Input
    style={{ marginTop: 16 }}
    name="content"
    label="Content"
    placeholder="Type the content"
  />
);

export default TextInput;
